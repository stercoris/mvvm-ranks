<UserControl 
    x:Class="Order.View.RankView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:converters="clr-namespace:Order.Converters"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" 
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:viewmodels="clr-namespace:Order.ViewModels"
    d:DesignHeight="60" d:DesignWidth="800"
    d:DataContext="{d:DesignInstance Type=viewmodels:RankItemViewModel, IsDesignTimeCreatable=False}">
    <UserControl.Resources>
        <converters:Base64ToImageSourceConverter x:Key="Base64ToImageSourceConverter"/>
        <converters:ColorConverter x:Key="ColorConverter"/>
    </UserControl.Resources>
    <Grid Background="{StaticResource DarkPurpleBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Foreground="{Binding Rank.Id, Converter={StaticResource ColorConverter}}" Text="{Binding Rank.Id}" Style="{StaticResource UserSelectionTitle}" FontSize="32"/>
        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Ellipse 
                Grid.Column="0" 
                Width="60"
                Height="60"
                Margin="0"
                Stretch="Fill">
                <Ellipse.Fill>
                    <ImageBrush 
                    ImageSource="{
                        Binding Rank.Picture,
                        Converter={StaticResource Base64ToImageSourceConverter},
                        ConverterParameter=100}">
                    </ImageBrush>
                </Ellipse.Fill>
            </Ellipse>
            <TextBlock
                Margin="10,0,0,0"
                Grid.Column="1"
                Text="{Binding Rank.Name}"
                Style="{StaticResource UserSelectionTitle}"
                FontSize="32"
                HorizontalAlignment="Left">
            </TextBlock>
        </Grid>
        
        <Button Grid.Column="2" HorizontalAlignment="Right" Style="{StaticResource Navigation}" Width="50" Height="50">
            <materialDesign:PackIcon Kind="ArrowDownThick" Style="{StaticResource NavBtnIcon}"/>
        </Button>
    </Grid>
</UserControl>
