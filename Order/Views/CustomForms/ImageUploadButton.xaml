<UserControl x:Class="Order.WPF.Views.CustomForms.ImageUploadButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Order.WPF.Views" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="100">
    <Button
        VerticalAlignment="Bottom"
        Width="auto"
        Height="auto"
        d:Height="100"
        HorizontalContentAlignment="Center"
        HorizontalAlignment="Right"
        Template="{StaticResource CustomButton}"
        Background="{x:Null}"
        BorderThickness="0"
        Click="LoadImage">
        <Border
            Background="{StaticResource PurpleBrush}" 
            CornerRadius="100"   
            Height="auto"
            Width="auto"
            BorderBrush="Black">
            <materialDesign:PackIcon 
                Style="{StaticResource NavBtnIcon}"
                Width="35"
                Height="35"
                Margin="10"
                HorizontalAlignment="Center"
                Kind="ImageEdit">
            </materialDesign:PackIcon>
        </Border>
    </Button>
</UserControl>
