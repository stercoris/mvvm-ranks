<UserControl x:Class="Ranks.View.RankView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:Ranks.ViewModels"
             d:DesignHeight="60" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewmodels:RankItemViewModel, IsDesignTimeCreatable=False}">
    <Grid Background="{StaticResource DarkPurple}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Rank.Id}" Style="{StaticResource UserSelectionTitle}" FontSize="32"/>
        <TextBlock Grid.Column="1" Text="{Binding Rank.Name}" Style="{StaticResource UserSelectionTitle}" FontSize="32"/>
        <Button Grid.Column="2" HorizontalAlignment="Right" Style="{StaticResource Navigation}" Width="50" Height="50">
            <materialDesign:PackIcon Kind="ArrowDownThick" Style="{StaticResource NavBtnIcon}"/>
        </Button>
    </Grid>
</UserControl>
