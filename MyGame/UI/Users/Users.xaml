<UserControl x:Class="Ranks.Users"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ranks"
             mc:Ignorable="d" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="450" d:DesignWidth="800" Height="570" Width="818">
    
    <StackPanel Orientation="Vertical" Background="White">
        <Grid>
            <ComboBox Name="groupList" Style="{StaticResource MaterialDesignComboBox}" ItemContainerStyle="{StaticResource MaterialDesignComboBoxItemStyle}" HorizontalAlignment="Right" Width="120" Margin="10,15,30,10" FontSize="17">
                
            </ComboBox>
        </Grid>
        <ScrollViewer Name="scroll" Height="510" Padding="25,0,0,0">
            <StackPanel Name="userList" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        </ScrollViewer>
    </StackPanel>
    

</UserControl>
